import { Vector } from "../Vector";
import { GivenData, MemoizedA } from "./GivenData";

export function optimizeAlphas(
    given: GivenData,
    A: MemoizedA,
    t̂: Vector[],
    t: number[][],
): number[][] {
    //!!
}
