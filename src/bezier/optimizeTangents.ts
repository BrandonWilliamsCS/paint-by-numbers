import { Vector } from "../Vector";
import { GivenData, MemoizedA } from "./GivenData";

export function optimizeTangents(
    given: GivenData,
    A: MemoizedA,
    α: number[][],
    t: number[][],
): Vector[] {
    //!!
}
